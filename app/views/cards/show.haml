.main__content
  %section.chapter__container
    %h2.chapter__head 支払い方法
    .payment__main
      .payment__list
        .payment__list__content
          %h3.sub__head クレジットカード一覧
      .card__payment__list
        %form.card__payment__content
        .card__list
          .card__list__picture  
        .card__number 
        
          = "**** **** **** " + card_url.slice(12..16)
        .expire__date 
          - exp_mouth = @expirationdate_mouth.to_s
          - exp_year = @expirationdate_year.to_s.slice(2,3)
          = exp_mouth + " / " + exp_year
        = form_tag(delete_credit_card_index_path, method: :post, id: 'charge-form',  name: "inputForm") do
          %input{ type: "hidden", name: "card_id", value: "" }
          %button.delete__button 削除する
      .how__not__regist
        %span 支払い方法について